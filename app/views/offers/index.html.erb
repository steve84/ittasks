<%= render "index" %>

<% if can?(:create, Offer) && current_user.id != @task.principal_id && @number_of_offers == 0 %>
	<%= link_to t('.new', :default => t("helpers.links.new")),
            new_task_offer_path(params[:task_id]),
            :class => 'btn btn-primary' %>
<% end %>
